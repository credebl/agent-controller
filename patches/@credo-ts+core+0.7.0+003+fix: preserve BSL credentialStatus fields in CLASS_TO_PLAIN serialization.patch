diff --git a/node_modules/@credo-ts/core/build/modules/vc/models/credential/W3cCredential.mjs b/node_modules/@credo-ts/core/build/modules/vc/models/credential/W3cCredential.mjs
--- a/node_modules/@credo-ts/core/build/modules/vc/models/credential/W3cCredential.mjs
+++ b/node_modules/@credo-ts/core/build/modules/vc/models/credential/W3cCredential.mjs
@@ -27,6 +27,8 @@ function CredentialStatusTransformer() {
 			if (record.type === BitStringStatusListEntry.type) return new BitStringStatusListEntry(record);
 			return new W3cCredentialStatus(record);
 		}
-		return value;
+		// CLASS_TO_PLAIN: spread to a plain object so BSL-specific fields (statusPurpose etc.) are preserved
+		if (value && typeof value === "object") return Object.assign({}, value);
+		return value;
 	});
 }
